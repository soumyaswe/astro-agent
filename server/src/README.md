# Server Auth

This folder contains Supabase authentication endpoints.

## Endpoints

- `POST /auth/signup`
- `POST /auth/signin`

## Environment

Set these variables in `.env`:

- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `PORT` (optional)
